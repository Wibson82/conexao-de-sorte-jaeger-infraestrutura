name: "📊 Jaeger Infrastructure – CI/CD Pipeline"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  SERVICE_NAME: jaeger-infrastructure

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4.3.0

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-yaml

      - name: Validate Docker Compose
        run: |
          # Validar sintaxe do docker-compose.yml
          docker compose -f docker-compose.yml config -q
          echo "✅ Docker Compose syntax is valid"

      - name: Security Validation
        run: |
          # Verificar se não há hardcoded passwords
          if grep -r "password.*:" docker-compose.yml | grep -v "\${" | grep -v "#" | grep -v "external:"; then
            echo "❌ Found potential hardcoded passwords"
            exit 1
          else
            echo "✅ No hardcoded passwords found"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4.5.0
        with:
          name: jaeger-configs
          path: |
            docker-compose.yml

  deploy-selfhosted:
    needs: validate-and-build
    runs-on: [ self-hosted, Linux, X64, conexao, conexao-de-sorte-jaeger-infraestrutura ]
    timeout-minutes: 15
    if: github.ref == 'refs/heads/main'
    env:
      DOCKER_NETWORK_NAME: conexao-network-swarm
      STACK_NAME: conexao-jaeger
      COMPOSE_FILE: docker-compose.yml
    steps:
      - uses: actions/checkout@v4.3.0
        with:
          clean: true
          fetch-depth: 1

      - name: Download artifacts
        uses: actions/download-artifact@v4.1.8
        with:
          name: jaeger-configs
          path: .

      - name: 🚀 Deploy Jaeger Stack (Swarm-Only)
        env:
          STACK_NAME: conexao-jaeger
          COMPOSE_FILE: docker-compose.yml
        run: |
          echo "🚀 Iniciando deploy do Jaeger com Docker Swarm..."

          # Remover stack existente se houver
          if docker stack ls | grep -q "$STACK_NAME"; then
            echo "🔄 Removendo stack existente '$STACK_NAME'..."
            docker stack rm "$STACK_NAME"
            sleep 8
          fi

          # Deploy da nova stack
          echo "🏗️ Executando deploy da stack '$STACK_NAME'..."
          docker stack deploy -c "$COMPOSE_FILE" "$STACK_NAME"

          echo "⏰ Aguardando estabilização dos serviços Jaeger..."
          sleep 35  # Jaeger inicialização otimizada

      - name: Healthcheck Jaeger (Via Logs)
        env:
          STACK_NAME: conexao-jaeger
        run: |
          echo "🔍 Validando saúde do Jaeger com health checks robustos..."

          # Aguardar até 3.3 minutos para Jaeger ficar disponível
          timeout=200
          elapsed=0
          health_passed=false

          while [ $elapsed -lt $timeout ] && [ "$health_passed" = false ]; do
            JAEGER_CONTAINER=$(docker ps -q -f name="${STACK_NAME}_jaeger" | head -1)

            if [ -n "$JAEGER_CONTAINER" ]; then
              echo "🔍 Testando health check do Jaeger... ($elapsed/$timeout segundos)"

              # Método 1: Verificar logs para "Query server started" ou "Jaeger started"
              if docker logs "$JAEGER_CONTAINER" 2>/dev/null | grep -q "Query server started\|Jaeger.*started\|server started.*jaeger"; then
                echo "✅ Jaeger health check passed (logs validation)"
                health_passed=true
                break
              fi

              # Método 2: Verificar se porta 16686 está ativa
              if docker exec "$JAEGER_CONTAINER" ss -tuln 2>/dev/null | grep -q ":16686 "; then
                echo "✅ Jaeger health check passed (port 16686 active)"
                health_passed=true
                break
              fi

              # Método 3: Fallback wget se disponível
              if docker exec "$JAEGER_CONTAINER" which wget >/dev/null 2>&1; then
                if docker exec "$JAEGER_CONTAINER" wget --quiet --tries=1 --timeout=3 --spider http://localhost:16686 2>/dev/null; then
                  echo "✅ Jaeger health check passed (wget validation)"
                  health_passed=true
                  break
                fi
              fi

              # Método 4: Verificar processo rodando
              if docker exec "$JAEGER_CONTAINER" ps aux 2>/dev/null | grep -q "[j]aeger\|[q]uery"; then
                echo "✅ Jaeger health check passed (process validation)"
                health_passed=true
                break
              fi

              echo "⏳ Jaeger ainda não está pronto... ($elapsed/$timeout segundos)"
            else
              echo "⏳ Container Jaeger ainda não encontrado... ($elapsed/$timeout segundos)"
            fi

            sleep 10
            elapsed=$((elapsed + 10))
          done

          if [ "$health_passed" = false ]; then
            echo "⚠️ Jaeger health check não passou em $timeout segundos"
            echo "🔍 Verificando logs finais do Jaeger..."
            if [ -n "$JAEGER_CONTAINER" ]; then
              echo "--- Últimos 30 logs do Jaeger ---"
              docker logs "$JAEGER_CONTAINER" --tail 30 2>/dev/null || true
              echo "--- Fim dos logs ---"
            fi
            echo "💡 Nota: Jaeger pode estar funcionando mesmo com health check parcial"
            echo "💡 Verifique os logs acima para diagnóstico"
            exit 1
          else
            echo "✅ Jaeger health check concluído com sucesso!"
          fi

      - name: Connectivity Validation
        env:
          STACK_NAME: conexao-jaeger
        run: |
          echo "🔗 Validando conectividade do Jaeger..."

          # Verificar se o serviço está listado no Swarm
          if docker service ls | grep -q "${STACK_NAME}_jaeger"; then
            echo "✅ Serviço Jaeger encontrado no Swarm"
          else
            echo "❌ Serviço Jaeger não encontrado no Swarm"
            exit 1
          fi

          # Verificar se está na rede correta
          if docker network inspect conexao-network-swarm | grep -q jaeger; then
            echo "✅ Jaeger conectado à rede overlay"
          else
            echo "⚠️ Jaeger pode não estar na rede overlay correta"
          fi

          # Testar endpoints adicionais (soft check)
          JAEGER_CONTAINER=$(docker ps -q -f name="${STACK_NAME}_jaeger" | head -1)
          if [ -n "$JAEGER_CONTAINER" ]; then
            # Testar endpoint Zipkin compatível (via porta)
            if docker exec "$JAEGER_CONTAINER" ss -tuln 2>/dev/null | grep -q ":9411 "; then
              echo "✅ Jaeger Zipkin endpoint ativo (porta 9411)"
            else
              echo "ℹ️ Jaeger Zipkin endpoint não detectado (pode ser normal)"
            fi
          fi

          echo "📊 Status final dos serviços:"
          docker service ls --filter name="${STACK_NAME}_*" --format "table {{.Name}}\t{{.Replicas}}\t{{.Image}}"